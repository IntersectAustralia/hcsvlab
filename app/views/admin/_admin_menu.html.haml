.sidenav.static.span3
  %h4 HCS vLab Admin
  %ul.nav
    - if can?(:read, User)
      %li{ :class => activepath?(users_path) }
        = link_to "List Approved Users", users_path
    - if can?(:access_requests, User)
      %li{ :class => activepath?(access_requests_users_path) }
        = link_to "View Access Requests", access_requests_users_path
    - if can?(:read, Licence)
      %li{ :class => activepath?(licences_path) }
        = link_to "Manage Licences", licences_path
    - if can?(:read, Licence)
      %li{ :class => activepath?(user_licence_requests_path) }
        = link_to "Manage Access To Collections", user_licence_requests_path
    %li{ :class => activepath?(view_metrics_admin_index_path) }
      = link_to "View Metrics", view_metrics_admin_index_path
