<% collection_name = @item.collection.name %>
<% item_name = @item.get_name %>
<%= link_to "Delete Item", delete_item_web_path(collection_name, item_name), :id => "delete_item_#{@item.id}", :class => "btn", data: {confirm: "Are you sure? This will delete the item '#{item_name}' and all its documents."} %>
